const { Army, attackStrategyEnums } = require('./army');
const { Battle, battleStatuses } = require('./battle');
const { BattleLog } = require('./battleLog');

module.exports = {
	Army,
	attackStrategyEnums,
	Battle,
	battleStatuses,
	BattleLog,
};
